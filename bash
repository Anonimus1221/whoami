1. Inicializa un nuevo repositorio y realiza el primer commit:

   ```bash
   cd whoami
   git init
   git add .
   git commit -m "Primer commit"

git remote add origin https://github.com/tu_usuario/whoami.git
git push -u origin master
